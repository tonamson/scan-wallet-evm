---
phase: 02-public-api-typings
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/index.d.ts
  - typecheck.ts
autonomous: true
requirements:
  - NATIVE-04
  - API-02
user_setup: []
must_haves:
  truths:
    - TypeScript consumers can import `scanNativeTransfers`, `NativeTransfer`, and `ScanNativeTransfersOptions` from the package root.
    - The public native types stay aligned with the runtime API and keep option parity with the existing ERC20 transport/provider fields.
    - Type checking proves the new native surface works without changing the existing ERC20 type contract.
  artifacts:
    - path: src/index.d.ts
      provides: Public native runtime declarations and exported native types
      min_lines: 180
      exports:
        - scanNativeTransfers
        - NativeTransfer
        - ScanNativeTransfersOptions
        - NATIVE_TRANSFER_DIRECTIONS
    - path: typecheck.ts
      provides: Consumer-style type usage coverage for the native API
      min_lines: 20
      contains: scanNativeTransfers
  key_links:
    - from: src/index.d.ts
      to: src/index.js
      via: declaration parity for public runtime exports
      pattern: scanNativeTransfers
    - from: typecheck.ts
      to: src/index.d.ts
      via: consumer-style compile-time verification of the native public API
      pattern: NativeTransfer
---

<objective>
Add the public TypeScript surface for native transfer scanning.

Purpose: Make the new native runtime API first-class for TypeScript users without altering the existing ERC20 type contract.
Output: Root-level native declarations, exported native types/constants, and compile-time usage coverage in `typecheck.ts`.
</objective>

<execution_context>
@/Users/tonamson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-public-api-typings/02-CONTEXT.md
@.planning/phases/02-public-api-typings/02-01-SUMMARY.md
@src/index.d.ts
@typecheck.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add native public declarations and exported types</name>
  <files>src/index.d.ts</files>
  <action>Update `src/index.d.ts` to declare the public native runtime API that Phase 2 introduced. Add dedicated exported types such as `NativeTransfer` and `ScanNativeTransfersOptions`, keeping the options shape as close as practical to `ScanErc20TransfersOptions` while omitting `tokenAddress`. Include `direction` in the public native result, keep `amount` as `bigint`, and export the selected native runtime constants/helpers that were exposed from `src/index.js`. Do not rewrite or rename existing ERC20 declarations just for symmetry.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>`src/index.d.ts` exports the public native function, native options/result types, and any selected native constants/helpers in a way that matches the runtime API.</done>
</task>

<task type="auto">
  <name>Task 2: Add consumer-style type usage coverage</name>
  <files>typecheck.ts</files>
  <action>Extend `typecheck.ts` with compile-time examples that import `scanNativeTransfers`, `NativeTransfer`, and `ScanNativeTransfersOptions` from the package root. Show that native scan options accept the shared provider/transport fields and that the existing ERC20 type-check path still compiles unchanged. Keep this focused on compile-time surface verification; runtime docs remain for Phase 3.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>`typecheck.ts` proves the new native public API and the old ERC20 API both compile cleanly from a consumer point of view.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passes
- [ ] TypeScript users can import `scanNativeTransfers`, `NativeTransfer`, and `ScanNativeTransfersOptions`
- [ ] Existing ERC20 declarations still compile without consumer-facing changes
</verification>

<success_criteria>

- All tasks completed
- Public native TypeScript declarations match the runtime API introduced in Plan 02-01
- Native typing surface is explicit, root-exported, and backward-compatible with the current package contract
- Consumer-style type checking proves the new native API and the existing ERC20 API can coexist cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/02-public-api-typings/02-02-SUMMARY.md`
</output>
