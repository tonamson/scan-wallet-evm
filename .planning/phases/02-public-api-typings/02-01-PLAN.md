---
phase: 02-public-api-typings
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.js
  - src/native.js
  - test/scan-native.test.js
autonomous: true
requirements:
  - NATIVE-04
  - API-01
user_setup: []
must_haves:
  truths:
    - JavaScript consumers can import `scanNativeTransfers` directly from the package root.
    - The public native scan accepts the same transport/provider options as the ERC20 scan and preserves the locked Phase 1 native behavior.
    - Existing ERC20 consumers continue using `scanErc20Transfers(...)` unchanged after the native export lands.
  artifacts:
    - path: src/index.js
      provides: Public root export for native transfer scanning
      min_lines: 120
      exports:
        - scanNativeTransfers
        - NATIVE_TRANSFER_DIRECTIONS
    - path: src/native.js
      provides: Internal native scan helpers consumed by the public wrapper
      min_lines: 80
      exports:
        - scanNativeTransfersWithProvider
    - path: test/scan-native.test.js
      provides: Runtime regression coverage for the public native API
      min_lines: 100
      contains: scanNativeTransfers
  key_links:
    - from: src/index.js
      to: src/native.js
      via: public wrapper delegating to internal native scan core
      pattern: scanNativeTransfersWithProvider
    - from: src/index.js
      to: createRpcProvider
      via: native scan transport/provider parity
      pattern: createRpcProvider
    - from: test/scan-native.test.js
      to: src/index.js
      via: package-root API import verification
      pattern: scanNativeTransfers
---

<objective>
Expose native transfer scanning as a stable runtime API at the package root.

Purpose: Deliver the public JavaScript surface for native scanning while keeping the Phase 1 semantics intact and leaving the ERC20 API untouched.
Output: Root-level `scanNativeTransfers` export, useful native scan constants/helpers, and runtime tests proving transport parity plus backward compatibility.
</objective>

<execution_context>
@/Users/tonamson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-public-api-typings/02-CONTEXT.md
@.planning/phases/01-native-scan-core/01-VERIFICATION.md
@src/index.js
@src/native.js
@test/scan-native.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root-level native scan runtime export</name>
  <files>src/index.js, src/native.js</files>
  <action>Update `src/index.js` to export `scanNativeTransfers` as a top-level peer of `scanErc20Transfers`. The public function must use an options object that mirrors the ERC20 calling style, reuses `createRpcProvider(...)` when `provider` is not injected, and forwards the locked native semantics from Phase 1 without broadening scope. Export a small set of useful native-related runtime constants/helpers from the package root, such as the native direction tuple, but do not create a noisy namespace and do not rename or reshape any ERC20 exports.</action>
  <verify><automated>node --input-type=module -e "import('./src/index.js').then((m) => { if (typeof m.scanNativeTransfers !== 'function') throw new Error('scanNativeTransfers export missing'); if (!Array.isArray(m.NATIVE_TRANSFER_DIRECTIONS)) throw new Error('NATIVE_TRANSFER_DIRECTIONS missing'); if (typeof m.scanErc20Transfers !== 'function') throw new Error('scanErc20Transfers changed'); })"</automated></verify>
  <done>`src/index.js` exposes `scanNativeTransfers` and selected native helpers at the package root while ERC20 exports continue to work unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove runtime parity and backward compatibility</name>
  <files>test/scan-native.test.js</files>
  <action>Refactor or extend `test/scan-native.test.js` so Phase 2 runtime coverage exercises the new public `scanNativeTransfers` export instead of only importing the internal module. Add assertions that the public wrapper accepts the same transport/provider options as ERC20 (`rpcUrl`, `proxy`, `proxyUrl`, `timeoutMs`, `provider`) and that existing ERC20 tests remain untouched. Keep README and declaration work out of this plan; this plan is about runtime behavior only.</action>
  <verify><automated>npm test</automated></verify>
  <done>Runtime tests prove the public native API works through the package root and that existing ERC20 behavior remains stable.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` passes
- [ ] `scanNativeTransfers` is importable from the package root
- [ ] Native runtime API reuses the existing transport/provider option style without breaking ERC20 consumers
</verification>

<success_criteria>

- All tasks completed
- Public JavaScript consumers can call `scanNativeTransfers(...)` from the package root
- The public native runtime API preserves Phase 1 native semantics and transport parity
- Existing ERC20 callers do not need migration changes
  </success_criteria>

<output>
After completion, create `.planning/phases/02-public-api-typings/02-01-SUMMARY.md`
</output>
